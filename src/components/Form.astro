<form class="form">
    <input type="text" placeholder="Ingresa tu nombre completo" class="form__input">
    <input type="email" placeholder="Ingresa tu correo" class="form__input">
    <textarea placeholder="Dime sobre el proyecto que tienes en mente..." class="form__input"></textarea>
    <button class="button">Enviar</button>
</form>

<style>
    .form {
        max-width: 450px;
        width: 100%;
        display: flex;
        flex-direction: column;
        gap: 20px
    }

    .form__input {
        padding: 1.25rem;
        font-family: Paragraph;
        font-size: 0.875em;
        background: none;
        color: var(--secundary-text);
        outline: none;
        border: 1px solid var(--accent);
        border-radius: 0.625rem;
        resize: none;
    }

    textarea.form__input {
        height: 100px;
    }

    .form__input::placeholder {
        color: var(--secundary-text);
    }
</style>